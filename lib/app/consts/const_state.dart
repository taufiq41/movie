enum SubmitState { initial, loading, success, failure }

enum DeleteState { initial, loading, success, failure }

enum UpdateState { initial, loading, success, failure }

enum ResendOtpState { initial, loading, success, failure }

enum BottomLoadState { initial, loading, loaded }

enum ChangePageState { initial, loading, success, failure }

enum OpenPageState { initial, loading, success, failure }

enum CancelState { initial, loading, success, failure }

enum SaveToFavoriteState { initial, loading, success, failure }

enum DeleteFromFavoriteState { initial, loading, success, failure }

enum LoadingState { initial, loading }

enum LogoutState { initial, loading, success, failure }
